import 'package:flutter/material.dart';

class CandyJar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("CandyJar"),
      ),
      body: Center(
        child: Text('CandyJar'),
      ),
    );
  }
}
